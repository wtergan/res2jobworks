# Web App

Static local dashboard renderer for MVP 1 evaluation and tracking.

The web surface renders semantic HTML from core command envelopes. It does not
own product state, write SQLite directly, or present a marketing landing page.
The first screen is the operational evaluation tracker: job table, current
status, score, and citation-backed evidence details.
